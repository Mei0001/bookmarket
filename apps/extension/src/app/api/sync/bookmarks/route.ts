import { notImplemented } from "@/lib/api/response";

export const dynamic = "force-static";

export async function POST() {
  return notImplemented("Sync API is scaffolded (client-side Chrome API for MVP).");
}

