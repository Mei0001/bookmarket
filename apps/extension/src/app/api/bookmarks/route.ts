import { notImplemented } from "@/lib/api/response";

export const dynamic = "force-static";

export async function GET() {
  return notImplemented("Bookmarks API is scaffolded (client-side IndexedDB for MVP).");
}

