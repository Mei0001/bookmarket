{
  "name": "bookmarket",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter ./apps/extension run dev",
    "build": "pnpm --filter ./apps/extension run build",
    "lint": "pnpm --filter ./apps/extension run lint",
    "test:unit": "pnpm --filter ./apps/extension run test:unit",
    "test:e2e": "pnpm --filter ./apps/extension run test:e2e",
    "pack:extension": "tsx tooling/scripts/pack-extension.ts"
  },
  "devDependencies": {
    "esbuild": "^0.27.1",
    "tsx": "^4.21.0"
  }
}
